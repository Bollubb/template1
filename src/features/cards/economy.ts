// Centralized economy knobs (single source of truth)

export const PACK_COST_BASE = 40;
export const PACK_COST_PREMIUM = 80;

// Premium pack bonus: how many extra rolls we add on top of the standard pack opening.
// Keep conservative to avoid inflation.
export const PREMIUM_EXTRA_ROLLS = 1;
